name: Github workflow with ArgoCD

on:
  workflow_call:
    inputs:
      service:
        required: true
        type: string
      image_tag:
        required: true
        type: string
    secrets:
      gh_pat:
        required: true

jobs:
  update-manifest-files:
    runs-on: ubuntu-latest
    env:
      GH_PAT: ${{ secrets.gh_pat }}
      SERVICE: ${{ inputs.service }}
      IMAGE_TAG: ${{ inputs.image_tag }}

    steps:
      - name: Checkout using Personal Access Token
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.gh_pat }}

      - name: Update Image Tag in Helm values.yaml
        run: |
          file="hipster/helm-chart/values.yaml"
          if [ ! -f "$file" ]; then
            echo "❌ File not found: $file"
            exit 1
          fi
          echo "🔄 Updating $file for $SERVICE..."
          sed -i "s|\(.*${SERVICE}.*tag:\).*|\1 \"${IMAGE_TAG}\"|" "$file"
          echo "✅ Updated image tag for $SERVICE to $IMAGE_TAG"

      - name: Commit and Push Changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git remote set-url origin https://x-access-token:${GH_PAT}@github.com/${{ github.repository }}
          git add .
          git commit -m "✅ Update $SERVICE to $IMAGE_TAG" || echo "Nothing to commit"
          git push origin HEAD:main
